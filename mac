        ========================================
            MacOS
        ========================================

# Set HOSTNAME
$ sudo scutil --set HostName localhost

# Install RVM
$ CC=/usr/bin/gcc-4.2 rvm install 1.8.7

# Flush DNS cache
$ dscacheutil -flushcache
